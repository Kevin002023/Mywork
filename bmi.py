# bmi.py
# Author: Kevin
# take inputs (weight, height) and calculate BMI.

kg = input("insert your weight (kilos) here: ")
metres = input("Insert your height (metres) here: ")

weight = float(kg)
height = float(metres)
#height =metres*100

bmi = kg/(metres)
#Print(bmi)